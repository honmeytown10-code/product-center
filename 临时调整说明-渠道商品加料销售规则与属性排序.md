# 临时调整说明：渠道商品加料销售规则与属性排序

> 文档性质：临时讨论与原型修改依据，不属于正式需求文档，不替代现有 PRD、接口文档或验收文档。
> 整理日期：2026-08-29

## 1. 调整背景

当前商品主档编辑页同时承载了以下两类配置：

1. 加料购买范围、起购/限购、必选等销售规则；
2. 规格、做法、加料等商品属性的展示顺序。

这两类配置都会因渠道经营方式和展示能力不同而产生差异，不应只能在商品主档统一配置。商品主档负责定义商品和属性结构，渠道商品负责当前渠道的销售表现与展示顺序。

本次临时调整包含：

- 在渠道商品编辑页补充加料限购配置；
- 将商品属性排序从商品主档移至渠道商品编辑页。

## 2. 调整一：渠道商品支持配置加料销售规则

### 2.1 职责划分

| 配置内容 | 商品主档 | 渠道商品 |
|---|---|---|
| 加料类型、加料商品及关联关系 | 定义和维护 | 继承，只允许启用/禁用，不允许新增、删除或修改结构 |
| 加料购买范围和数量规则 | 作为品牌默认规则 | 可针对当前渠道调整 |
| 单项加料价格、渠道启用状态等渠道销售值 | 不替代渠道配置 | 按当前渠道维护 |

渠道可以调整销售规则，不代表渠道可以改变加料结构。渠道商品仍不得新增、删除、改名或重新编排主档中的加料类型和加料商品关系。

### 2.2 渠道商品编辑页调整

在渠道商品编辑页的“加料”模块内、加料分组列表之前增加“加料销售规则”区域，控件和校验口径与商品主档保持一致：

- 加料配置范围：
  - 限制所有加料购买总量；
  - 按加料类型限制购买数。
- 自由选择加料的数量规则：
  - 点餐时数量不限；
  - 点餐时起购限购数；
  - 点餐时必选。
- “点餐时起购限购数”按照现有口径填写起购数和限购数，并校验起购数不得大于限购数。
- 按加料类型限制时，各加料类型分别配置自由选择或固定加料及其数量规则。
- 已在当前渠道禁用的加料不参与当前渠道的可售数量计算，但不从主档结构中删除。

### 2.3 继承与覆盖建议

临时方案建议采用“主档默认值 + 渠道覆盖值”的方式：

1. 渠道商品首次生成时，使用商品主档当时的加料销售规则作为初始值；
2. 渠道未做独立调整时，页面显示主档默认规则及来源；
3. 渠道修改并保存后，仅影响当前渠道商品，不回写商品主档，也不影响其他渠道商品；
4. 已形成渠道覆盖值后，商品主档后续修改不自动覆盖该渠道；
5. 页面提供“恢复主档默认规则”操作，执行前明确将被替换的当前渠道配置。

> “未覆盖渠道是否持续跟随主档最新值”涉及存储与发布语义，正式落地前需与接口方案一起确认。若暂不实现持续继承，可在渠道商品创建时复制主档值，后续全部按渠道独立保存。

### 2.4 保存与反馈

- 保存渠道商品时同时校验当前渠道的加料启用状态与销售规则；
- 规则不完整或起购/限购关系错误时，定位到对应加料类型并阻止保存；
- 保存成功后提示“当前渠道加料销售规则已更新”；
- 变更记录应包含修改前后值、渠道商品库、操作人和时间；
- 无渠道商品编辑权限时只读展示，不将无权限伪装为字段缺失。

## 3. 调整二：商品属性排序移至渠道商品

### 3.1 职责划分

| 配置内容 | 商品主档 | 渠道商品 |
|---|---|---|
| 规格、做法、加料的定义和关联结构 | 定义和维护 | 继承，不修改结构 |
| 属性组展示顺序 | 不配置 | 按当前渠道配置 |
| 属性值展示顺序 | 不配置 | 按当前渠道配置 |
| 渠道属性启用状态 | 不替代渠道配置 | 仅对继承项启用/禁用 |

商品主档负责“有哪些属性和属性值”，渠道商品负责“当前渠道以什么顺序展示”。不同渠道商品可以保存不同顺序，互不影响。

### 3.2 商品主档页面调整

- 从商品主档创建、编辑页移除“属性排序 / 商品属性排序”模块；
- 商品主档仍维护规格、做法、加料的结构和关联关系；
- 本次仅迁移排序职责，不因移除排序模块改变商品主档中的属性定义、默认校验或关联数据。

### 3.3 渠道商品编辑页调整

在渠道商品编辑页增加“商品属性排序”模块，建议放在规格、做法、加料配置之后，作为渠道展示配置的一部分。

支持：

- 调整规格、做法组、加料组之间的展示顺序；
- 调整同一属性组内属性值的展示顺序；
- 仅展示当前渠道可用的属性和属性值；
- 拖拽排序时提供明确拖拽手柄；
- 开启自定义排序后显示“有未保存修改”，并提供保存、取消或恢复默认顺序；
- 保存后只更新当前渠道商品的排序结果，不回写商品主档和其他渠道商品。

### 3.4 属性变更处理

- 主档新增属性组或属性值后，新增项默认追加到对应渠道现有顺序末尾；
- 主档删除属性或解除关系后，渠道排序中同步移除失效项；
- 渠道禁用某个属性或属性值后，该项不在消费者端展示，但保留其排序位置，重新启用时恢复原位置；
- 渠道尚未保存自定义排序时，按系统默认顺序展示；
- 排序数据失效或保存冲突时保留用户当前调整，并提示刷新或重新提交。

### 3.5 默认值配置边界

当前“商品属性排序”模块同时出现默认规格值、默认加料等能力，但本次明确调整的是“排序归属”。为避免顺带改变销售语义，临时方案先按以下边界处理：

- 属性组顺序和属性值顺序迁移到渠道商品；
- 默认规格值、默认做法、默认加料的归属和继承方式不在本次调整中直接改动；
- 若原型继续将默认值选择与排序放在同一模块，应在正式实施前确认默认值是否也需要成为渠道覆盖项，再决定一起迁移或拆成独立配置。

## 4. 历史数据处理建议

1. 加料销售规则：现有商品主档规则作为渠道商品的初始默认值，不丢失现有配置；
2. 属性排序：将现有商品主档排序一次性复制到其已存在的渠道商品，保证迁移前后消费者端顺序不突变；
3. 完成复制后，商品主档不再提供排序入口，后续排序只从渠道商品维护；
4. 无对应渠道商品的数据不生成孤立排序记录；后续创建渠道商品时按系统默认顺序初始化；
5. 迁移过程记录成功、失败和跳过数量，失败项支持重试或导出。

## 5. 原型修改清单

### 商品主档创建/编辑页

- 保留加料结构维护和品牌默认加料销售规则；
- 移除“属性排序 / 商品属性排序”模块；
- 删除与“在商品主档统一配置属性顺序”相关的说明文案。

### 渠道商品编辑页

- 在加料模块补充“加料销售规则”；
- 规则可按当前渠道保存，结构类操作继续禁用；
- 增加“商品属性排序”模块；
- 标识配置作用范围为当前渠道商品库/当前渠道商品；
- 补充继承来源、恢复默认、保存校验和失败反馈。

## 6. 临时验收口径

- [ ] 商品主档页面不再出现商品属性排序入口；
- [ ] 渠道商品编辑页可以调整属性组顺序和组内属性值顺序；
- [ ] 两个渠道可以为同一主档商品保存不同属性顺序；
- [ ] 渠道商品编辑页可以配置加料购买范围及对应数量规则；
- [ ] 渠道调整加料销售规则不会回写商品主档或影响其他渠道；
- [ ] 渠道仍不能新增、删除或修改加料类型和加料商品结构；
- [ ] 禁用加料不参与当前渠道可售数量计算，重新启用后恢复原结构；
- [ ] 保存前完成数量、对象有效性和权限校验；
- [ ] 历史主档排序和加料规则迁移后不造成现有渠道展示突变；
- [ ] 页面明确展示当前配置作用的渠道商品库与保存结果。

## 7. 正式实施前待确认

1. 未设置渠道覆盖值时，是持续跟随主档最新加料规则，还是仅在渠道商品创建时复制一次；
2. 默认规格值、默认做法、默认加料是否与排序一起迁移为渠道配置；
3. 不同渠道能力不一致时，无法下发的加料数量规则如何降级及提示；
4. 已发布门店商品是否立即使用新规则，还是需要重新发布后生效；
5. 历史属性排序一次性迁移的执行范围、失败重试与审计方式。
