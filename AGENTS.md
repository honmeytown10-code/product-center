# 全渠道商品中心 UI / 交互优化工作树规则

## 工作树边界

- 本工作树路径：`E:\code\product-center\.codex_tmp\worktrees\full-channel-ui`。
- 本工作树分支：`codex/full-channel-ui-optimization`。
- 业务基线来自：`E:\code\product-center\.codex_tmp\worktrees\full-channel` 的当前完整工作区状态，而不只是其最后一次提交。
- 原 `full-channel` 工作树和 `main` 只作为对照，不在本任务中修改。

## 本轮唯一目标

在现有全渠道商品管理业务逻辑和字段基线之上，系统性重构 PC 后台的信息架构、页面 UI、交互流程、状态反馈、权限表达以及弹窗/抽屉体验；用户后续明确确认的业务变更同步沉淀到本工作树。

## 业务字段冻结规则

除非用户明确确认业务变更，否则不得新增、删除、改名、合并或重解释以下内容：

- 商品、SKU、分类、属性、配方、渠道商品、门店商品及全渠道配置的业务字段。
- 商品类型、类目选项、枚举值、默认值、校验规则、字段联动和对象边界。
- 已有菜单能力、路由能力、操作权限、状态含义和保存语义。

特别约束：

- 创建商品只保留现有“标准商品”和“套餐商品”两种类型。
- 两种商品类型各自可选类目、表单字段、显示条件和校验必须完全沿用原实现；不得凭原型或设计文档重新发明。
- “商品主档”和“渠道商品”的字段以现有实现为准，UI 优化不能顺便重构数据模型。
- 全渠道配置只允许围绕现有协作方式、三方渠道维护位置、企迈参与能力、渠道商品库分组、门店连接派生状态及现有保存校验优化呈现和交互。
- 不得在全渠道配置中自行增加负责人、SLA、巡检默认值、默认发布规则等未确认字段。

## 业务事实来源优先级

发生冲突时按以下顺序处理：

1. 本工作树当前代码和运行效果。
2. 原 `full-channel` 工作树当前代码和运行效果。
3. `design-handoff/` 中基于现有代码梳理的资料。
4. 商品中心 Next / Console 设计技能中的架构和交互建议。
5. 通用设计经验或原型模拟数据。

关键事实文件：

- `components/web/WebProductForm.tsx`
- `components/web/WebProductList.tsx`
- `components/web/WebChannelProductLibrary.tsx`
- `components/web/WebOmnichannelSettings.tsx`
- `components/WebAdmin.tsx`
- `omnichannel.ts`
- `types.ts`

设计建议不得覆盖代码中的真实业务事实。若两者不一致，先记录问题并请求用户确认，不直接修改业务字段。

## 已确认的信息架构与页面差异

采用六个任务域：商品工作台、商品资料、渠道经营、发布与治理、门店执行、配置。菜单显示可随现有管理策略联动，但不得因此丢失能力。

不同业务页面采用不同骨架：

- 商品工作台：任务分组 + 任务列表 + 批量操作坞 + 覆盖式详情抽屉。
- 商品管理/主档：状态视图 + 紧凑筛选 + 分类树 + 高密度表格。
- 分类与属性：对象树/分组 + 列表或配置面板。
- 渠道商品：渠道商品库上下文 + 主档选择 + 渠道资料维护。
- 发布中心：来源/目标 + 预检 + 确认 + 批次结果。
- 映射/差异治理：风险清单 + 双对象/字段对照 + 闭环处置。
- 门店商品：门店/渠道范围 + 高密度商品表 + 高频批量经营操作。
- 配置页面：配置分组 + 稳定表单 + 变更影响说明。

不得把所有菜单页面统一套成同一种表格或卡片布局。

## 2026-08-06 已确认的策略、创建与权限决策

- 统一管理和分渠道协作保持同一套菜单结构，均显示“商品主档”“渠道商品”“渠道分类与属性”；统一管理仅把渠道商品库收敛为一个品牌默认商品库。
- 渠道商品库的商品创建方式为品牌级策略：“仅选择已有商品主档”或“允许同时创建主档与渠道商品”。两个协作模式均可启用；统一管理推荐开启，分渠道协作按职责选择。
- 同时创建不是新对象类型。仍只允许标准商品、套餐商品，并继续使用其原有类目、字段和校验；表单只按主档资料与渠道商品资料分区，一次提交分别写入主档和当前渠道商品库。
- 渠道商品库具有独立数据权限范围。正式方案必须拆分页面权限、商品库数据范围和操作权限；拥有渠道商品菜单权限不代表可见全部商品库。
- 原型暂按当前用户拥有全部商品库权限展示，无需增加权限配置页面；方案、需求、接口和验收必须覆盖无范围、部分范围、跨商品库操作与模式切换时的权限迁移。

## 已确认的公共交互

- 工作台右侧详情抽屉必须覆盖在内容之上，不能挤压、缩窄或重排原列表和工具栏。
- 左侧菜单和页面暂不显示可见滚动条；内容溢出时仍保留滚轮、触控板和键盘滚动能力。
- 抽屉用于保留列表上下文的查看或轻编辑；弹窗用于单一短任务；复杂长表单进入独立页面。
- 批量选择后显示稳定操作区和影响范围；部分成功必须展示成功、失败、等待及失败原因。
- 危险操作必须说明对象、渠道/门店范围、生效时间、后果、恢复方式和审计结果。
- 无权限不得伪装为空数据；字段权限、动作权限和范围权限分别表达。
- 每页至少覆盖 loading、success、first-empty、filtered-empty、error、no-permission、partial-permission、submitting；适用时增加 partial-success、并发冲突、授权失效和异步等待。

## 工作台任务机制边界

当前工作台只确认了 UI 骨架，任务创建、负责人解析、SLA、升级、提醒和转交权限尚未最终确认。可以按“系统内置领域任务 + 可配置职责路由”继续设计，但不得在页面或模拟数据中把负责人规则写成最终业务事实。

## 2026-08-09 已确认的门店售卖规则交互

- 列表不展示“影响范围”“命中对象”等执行术语；导出入口命名为“导出规则”，导出规则对象、经营结果、门店、渠道和状态。
- 状态列只显示完整可见的真实开关，通过 Tooltip 与无障碍名称表达“已启用/已停用”，启停前二次确认；创建、编辑和启用均前置校验冲突。“更多”菜单必须使用不受表格滚动区域裁切的浮层。
- 门店售卖规则区分四类对象：商品直接多选；规格按商品选择完整 SKU 组合；做法、加料直接选择公共定义并默认覆盖全部关联商品，也可收窄到指定商品。新增/编辑按“管控对象 → 门店与渠道 → 校验确认”三步完成，不在抽屉中平铺全部子项。
- 未配置门店售卖规则时默认全部适用门店可售/可选，因此新建规则不提供“全部适用门店可售/可选”；两种局部门店结果均使用统一门店选择器，表单只回显数量和部分名称。
- 状态列必须完整展示开关和文字。规则支持删除，但启用规则必须先停用；删除二次确认并保留审计，已发布商品状态不自动回滚。

## 2026-08-11 已确认的分类归属与售卖对象决策

- 前台分类与后台分类的定义均归商品主档维护。“分类与属性”同时管理两类分类，商品主档创建/编辑表单填写默认前台分类；渠道商品默认继承主档，但可从已有启用分类中覆盖当前商品库归属，不回写主档或影响其他商品库。
- 前台分类是商品中心共享分类体系，供小程序、POS、三方渠道及营销、卡券等下游查询；查询当前商品库时使用“渠道覆盖值 > 主档默认值”，历史无商品库上下文的下游继续读取主档默认值。三方平台类目仍属于渠道/平台专属映射字段，不与前台分类合并。
- 门店售卖规则将“商品”和“规格”合并为一个“商品 / SKU”管控对象。选择商品后默认管控其全部当前有效 SKU；仅需控制部分规格时，再对该商品切换为“指定 SKU”并选择完整 SKU 组合。
- 冲突校验、命中计算、导出和审计均按最终展开后的 SKU 集合执行。商品新增 SKU 时，“全部 SKU”规则自动包含新增有效 SKU；“指定 SKU”规则不自动扩展。

## 全量覆盖与交付粒度

所有正式菜单、页内视图和子页面均在本轮范围内。分类与属性、渠道分类/属性、四类销售规则、门店分类/属性、开放接口不得遗漏。

每个设计单元必须细化到：

- 页面目标、角色、对象、范围和上下游。
- 筛选项、表格列、详情字段、表单字段及显示/默认/校验/权限规则。
- 页面按钮、行操作、批量操作和更多菜单的显示、禁用、加载、成功及失败规则。
- 弹窗、抽屉、向导、确认、未保存离开和失败恢复。
- 正常、空、错误、权限、并发、部分成功和异步状态。
- 1440px 下的真实长文案、长路径、横向滚动和固定操作区验收。

在质量不下降的前提下，同一业务域、共享对象和共享组件的 3–5 个页面可作为一批完成；不跨越尚未确认的业务边界强行凑批次。

## 参考资料

- `design-handoff/04-conversation-ui-baseline.md`：本次对话确认项、否决项和后续批次。
- `design-handoff/05-skill-inventory.md`：工作树内领域 Skill 与 Codex 全局外部设计 Skill 的版本、用途和调用顺序。
- `design-handoff/reference/product-workbench-approved.png`：用户已基本认可的工作台视觉参考，仅作为 UI 证据，不作为业务字段来源。
- `.agents/skills/product-center-next-design/`：菜单、对象、流程与迁移方法。
- `.agents/skills/product-center-console-design/`：Console 2.0 视觉、组件、交互和验收规范。
